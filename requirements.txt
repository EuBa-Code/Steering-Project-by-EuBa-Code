# Core dependencies (see pyproject.toml for the canonical list)
# Install PyTorch for your CUDA version first: https://pytorch.org/get-started/locally/
torch>=2.0
transformers>=4.30
accelerate
numpy

# Development / notebooks
jupyter
matplotlib
